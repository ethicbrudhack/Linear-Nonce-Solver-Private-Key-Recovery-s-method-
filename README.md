# Linear-Nonce-Solver-Private-Key-Recovery-s-method-
This snippet checks whether two ECDSA signatures reveal a **linear relation** between their nonces and — if so — directly recovers the private key. 
